# Movement System Addon

Reusable Godot 4.6 movement system addon.

## Install

Copy this folder into your target Godot project root:

- `addons/movement_system` (contains all movement system scripts/assets)

Then enable the plugin:

`Project Settings -> Plugins -> Movement System`
