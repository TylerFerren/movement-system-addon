# Movement System Addon

This addon registers the movement system classes as custom node types.

## Enable

1. Open `Project Settings -> Plugins`.
2. Enable `Movement System`.

## Reuse In Another Project

Copy this folder into the target project:

- `res://addons/movement_system` (contains all movement system scripts/assets)

Then enable the plugin in that project.
